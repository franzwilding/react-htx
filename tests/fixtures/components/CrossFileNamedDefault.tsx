import { ImportedComponent } from "./ImportedComponent";

// Named-import re-exported as default — exercises:
// 1. resolveIdentifierToProps's loop over imports (named-import match)
// 2. resolveImportedComponent for a named export.
export default ImportedComponent;
