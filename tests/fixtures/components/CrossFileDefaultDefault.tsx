import ImportedDefault from "./ImportedComponent";

// Default-import re-exported as default — exercises the default-import
// branch of resolveIdentifierToProps and resolveImportedComponent.
export default ImportedDefault;
